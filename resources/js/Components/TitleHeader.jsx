const TitleHeader = ({ text }) => {
    return (
        <h2 className="mb-4 text-lg font-bold text-gray-700 dark:text-black text-center">
            {text}
        </h2>
    );
};

export default TitleHeader;
